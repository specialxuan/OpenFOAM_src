# 快速动网格程序手册

## 版本说明

- 目标库：`fastDynamicFvMesh`（位于 `fastDynamic`/`fastDynamicFvMesh`）
- 作者/维护者：Special.轩
- 适用 `OpenFOAM` 版本：用户环境下的 `OpenFOAM` v2412

## 目录

- [快速动网格程序手册](#快速动网格程序手册)
  - [版本说明](#版本说明)
  - [目录](#目录)
  - [概述](#概述)
  - [构建与安装](#构建与安装)
  - [运行与调用流程（高层）](#运行与调用流程高层)
  - [配置文件说明（case 端）](#配置文件说明case-端)
  - [输入/输出文件格式](#输入输出文件格式)
  - [`fastDynamicFvMesh` 类：函数与变量说明（详尽）](#fastdynamicfvmesh-类函数与变量说明详尽)
  - [关键实现流程（每步的数据流与要点）](#关键实现流程每步的数据流与要点)
  - [故障排查与调优建议](#故障排查与调优建议)
  - [结束语](#结束语)
  - [附录 A：示例 `dynamicMeshDict` 与 `controlDict` 片段](#附录-a示例-dynamicmeshdict-与-controldict-片段)
  - [附录 B：源代码（注释已翻译为中文）](#附录-b源代码注释已翻译为中文)

## 概述

`fastDynamicFvMesh` 实现了一种模态驱动的快速动态网格方法（modal-based dynamic mesh）。它通过读取结构模态（模态形状、频率、归一化约定），将流体在界面（`fsiPatches`）上的力投影到模态坐标，解模态动力学方程得到模态位移，再将模态位移重建为界面节点位移并更新网格（`movePoints`）。同时记录逐模态的压力/剪切贡献并可写出面级诊断数据以定位力的空间分布。

## 构建与安装

前置：先在 shell 中 source 对应 `OpenFOAM` 环境（例如 source /opt/openfoam\*/etc/bashrc 或用户本地环境）。

从仓库根目录（`fastDynamic` 的父级，通常为 /root/OpenFOAM/user-v2412/src/fastDynamic）执行：

wmake libso

说明：

- 该命令使用 `OpenFOAM` 的 wmake 构建动态库 `libfastDynamicFvMesh.so`。
- 若报错，检查环境变量 `FOAM_INST_DIR`、编译器与 `OpenFOAM` 匹配。

安装/在 case 中启用：在 case 的 system/`controlDict` 中加入：

`libs ("libfastDynamicFvMesh.so")`;

在 case 的 constant/`dynamicMeshDict` 中启用方法，并配置本手册中列出的控制项：

`dynamicFvMesh` `fastDynamicFvMesh`;

## 运行与调用流程（高层）

- case 启动（`pimpleFoam` 或用户指定的求解器），`OpenFOAM` 的 `dynamicFvMesh` 工厂会根据 `dynamicMeshDict` 实例化 `fastDynamicFvMesh`。
- 构造函数（class 构建期间）调用 `readControls()`、`readModeShapes()` 等以加载模态数据与映射表。
- 每个时间步：`dynamicFvMesh` 基类在求解器时间循环中调用 `update()`；`fastDynamicFvMesh`::`update()` 按顺序执行：采样流体界面力、投影到模态、解结构模态方程、松弛应用（`couplingRelaxation`），重建节点位移并调用 `movePoints()` 更新网格、写诊断（`modal_diagnostics.csv`、可选面级文件）。

## 配置文件说明（case 端）

建议在 constant/`dynamicMeshDict` 中包含以下控制项（关键项）：

- `fsiPatches`: 列表，指定流体-固体交界的 patch 名称（必需）。
- `couplingRelaxation`: 取值 $(0,1]$，默认 `0.1`。对显式耦合情形的位移分量进行松弛以改善稳定性（已在代码中支持）。
- `faceDiagnosticsMode`: 整数或 -1（禁用），若 >=0 则写出指定模态的面级诊断文件（按 processor 划分）。
- `mappingTolerance`: 映射坐标容差（用于 `CSV`→网格的节点映射）；若出现映射丢失，可增大容差。
- `modalFilesPath` / `modeFilesPrefix`: 指向模态数据（频率、形状向量）文件的路径（参见 5 节）。

另外：`controlDict` 需要 `libs("libfastDynamicFvMesh.so")`，并在 `fvSolution` 中保留必要的 `pcorr/projection` 配置（动态网格运行惯例）。

## 输入/输出文件格式

必需输入（放在 case 根目录或 `dynamicMeshDict` 指定的位置）：

- `FluidNodeCoor.csv`：流体界面节点坐标（`CSV`），用于将提供的结构模态节点映射到流体网格。注意：legacy 格式的 header 可能不同，代码包含对不同旧格式的兼容处理，但应尽量使用标准格式：第一行说明，随后每行 `nodeID`,x,y,z。
- `FluidNodeDisp\*.csv`：每一模态的节点位移文件（或单一合并文件），用于读取模态形状。模态是归一化的（用户已说明），代码假定模态向量按节点排列并与 `FluidNodeCoor` 匹配。
- `FluidPara.csv`（可选）：包含模态质量、阻尼等参数（若不存在，代码默认 `modalMass=1`，`modalDamping=0`；建议补充以获得物理结果）。

输出：

- `modal_diagnostics.csv`：每时间步写入的逐模态诊断，包含 `Time`、`Force_i`、`PressureForce_i`、`ShearForce_i`、`Disp_i`、`Vel_i`、`Acc_i`、`AppliedDisp_i`（已记录经松弛后的应用位移）。
- `faceDiagnostics_mode{N}_proc{P}.csv`（当 `faceDiagnosticsMode=N` 启用）：每处理器写出的面级诊断，列出面索引、局部坐标、`pressureContribution`、`shearContribution` 等，用于定位贡献热点。

## `fastDynamicFvMesh` 类：函数与变量说明（详尽）

以下以类成员名为准（在 `fastDynamicFvMesh.H`/`C` 中定义），按功能模块分类。若代码在不同版本中变量名微动，请以源码为准，本手册列举当前实现中的主要符号与用途。

构造/析构与初始化

- `fastDynamicFvMesh(const fvMesh& mesh, const dictionary& dict)`
  - 作用：构造对象，读取 `dynamicMeshDict` 控件并完成初始化。调用 `readControls()`、`readModeShapes()`、建立映射表。

- ~`fastDynamicFvMesh()`
  - 作用：析构时释放文件句柄、关闭诊断文件。

设置/读取

- void `readControls()`
  - 作用：从 `dynamicMeshDict` 读取控制项，初始化 `couplingRelaxation`_、`faceDiagnosticsMode`_、`mappingTolerance_`、`modalFilesPath` 等。
  - 关键变量：`couplingRelaxation`_（double）、`faceDiagnosticsMode`_（int）、`mappingTolerance`_（double）、`diagnosticsDir`_（string）

- void `readModeShapes()`
  - 作用：解析模态文件（如 `FluidNodeDisp\*.csv`），读取 `modalShapes*（vector<vector<double>>` 或 `scalarField` 的集合）、`modalFrequencies*（vector<double>）`、`nModes*`、`nNodes`\*。
  - 关键变量：`modalShapes*`（每模态的节点位移向量集合）、`modalFrequencies*`（角频率或频率）、`modalMass*`（可选读取或默认 `1.0`）、`modalDamping*`（可选或默认 `0.0`）

映射与几何

- void `buildMapping()`
  - 作用：将结构节点（`CSV`）映射到流体网格节点/面。生成 `fluidNodeMap*`（`structureNodeID` -> `meshPointID`）和 `faceToNodeMap*` 以便后续位移重建。
  - 关键变量：`fluidNodeMap*`（map）、`mappingTolerance*`（double）

力投影与积累

- void `calcModalForces(const scalar time)`
  - 作用：对当前流体场（压力、剪切力）在 `fsiPatches` 上的每个面进行遍历，将面力投影到模态基底，分别累加到 `modePressureForce*[m]` 与 `modeShearForce*[m]`。
  - 关键变量：`modePressureForce*`（`vector<vector<scalar>>` 或 `vector<scalar>` 随模态）、`modeShearForce*`；face-level临时变量：`facePressure`, `faceShear`, `faceAreaVec`
  - 注意：代码内部对面法向以及剪切投影使用精确的力分解公式，确保压力贡献与剪切贡献分离。

结构动力学求解

- void `solveStructuralDynamics(scalar dt)`
  - 作用：对每个模态 `m` 求解 $M_m * \ddot{x} + C_m * \dot{x} + K_m * x = F_m$ ，其中 $K_m$ 约定为 `modalFrequencies_[m]^2`（若模态频率是角频率），`M_m`、`C_m` 从输入或默认值。数值积分方法：可能使用 `Wilson`-`theta` 或 `Newmark`（实现中为 `Wilson`-`theta` 单步公式，参阅代码注释）。
  - 关键变量：`appliedModeDisp*`（每模态经松弛后实际应用的位移，用于诊断）、`modeDisp*`（当前模态位移）、`modeVel*`、`modeAcc*`、`modalMass*`、`modalDamping*`
  - 注意：若 `modalMass*/modalDamping*` 未提供，默认值会影响物理正确性，建议在输入文件中指定。

网格更新与写出

- void `applyRelaxationAndUpdate()`
  - 作用：将结构求解产生的模态位移按 `couplingRelaxation*` 与上一时刻 `appliedModeDisp*` 混合，得到新的 `appliedModeDisp_ = alpha * newDisp + (1-alpha) * oldApplied`。然后从模态位移重建为节点位移并调用 `movePoints()`。
  - 关键变量：`couplingRelaxation*`（alpha, double）、`appliedModeDisp*`（vector）

- void `movePoints()`
  - 作用：根据重建的节点位移更新 `mesh.points`()，触发 `OpenFOAM` 的边界/网格更新子系统。

诊断输出

- void `writeModalDiagnostics(scalar time)`
  - 作用：向 `modal_diagnostics.csv` 写入本时间步每模态的 `Force_i`、`Pressure_i`、`Shear_i`、`Disp`、`Vel`、`Acc`、`AppliedDisp` 等列。

- void `writeFaceDiagnostics(int mode, int proc)`
  - 作用：当 `faceDiagnosticsMode_ == mode` 时，为每一面输出面级 `pressureContribution`、`shearContribution`、面位置等，文件按 processor 划分。

工具/辅助函数（常见的私有成员）

- `mapLookup`/`nearestPoint` 搜索函数：用于把结构节点定位到流体网格点或面中心。
- `parseCSVLine`/`legacyCSVHandler`：兼容不同格式的模态/坐标 `CSV` 解析器。
- `validateMapping()`: 验证映射完整性，若有节点未映射则报错或警告。

## 关键实现流程（每步的数据流与要点）

初始化（构造中）：

- 读控件 -> 读模态文件（`modalShapes*` + `modalFrequencies*`）-> 建立结构节点到流体网格的映射（`fluidNodeMap_`）-> 打开诊断文件。

每个时间步（update）:

1. 遍历 `fsiPatches` 中的每个面：读取 pressure、`wallShearStress`（或基于速度梯度计算剪切），计算面力向量。
2. 将每个面力按面作用点插值到映射到的结构节点（或直接以面贡献投影到模态基底）。
3. 对所有节点/面贡献执行模式投影：$F*m = \sum_{nodes} \phi_m^T * f_{node}$，分别记录 pressure 与 shear 的分量。
4. 用 `solveStructuralDynamics(dt)` 得到 `modeDisp*`, `modeVel*`, `modeAcc_`。
5. 用 `applyRelaxationAndUpdate()`：`appliedModeDisp_ = alpha * modeDisp + (1-alpha) * appliedModeDisp_previous`。
6. 重建节点位移并调用 `movePoints()`，更新网格几何。
7. 写出 `modal_diagnostics.csv` 与可选 `faceDiagnostics` 文件。

## 故障排查与调优建议

常见错误与修复建议：

- 映射失败（某些结构节点找不到流体对应点）：检查 `FluidNodeCoor.csv` 的坐标系与单位是否匹配流体网格，增加 `mappingTolerance`。
- `FPE` 或求解器崩溃（`GAMG`/`PCG` 遇到非数）：通常是耦合不稳定导致压力失控。策略：降低 `couplingRelaxation`（例如 `0.1`）、增加模拟阻尼 `modalDamping*`、或为模态指定更大的 `modalMass*`。对于严重情况，建议实现 `Aitken` 或子迭代耦合。
- 模式贡献符号/方向异常：检查模态归一化与位移方向约定（positive outward ?）。对照 `Fluent` `UDF`（提供的 `fdmUtils.c`）确认投影公式的法向方向与 sign convention。
- `Mode 4` 大幅压力/剪切抵消但总和小：启用面级诊断（`faceDiagnosticsMode=4`）定位贡献带并检查每面 pressure 与 shear 的方向与投影是否一致。

性能调优：

- 投影操作可并行化（按面/按模态分块），若是多核/并行运行可利用 `OpenFOAM` 的并行通信对投影结果做归约。
- 避免频繁开/关大文件，诊断输出使用缓冲并在时间步间合并写入以减少 I/O 开销。

## 结束语

本手册旨在为工程师和维护人员提供对 `fastDynamicFvMesh` 实现的完整理解、部署与调试指南。若需要将本代码投入更严格的工程验证流程，建议：

- 将模态质量/阻尼完整读入并用于动力学方程（避免 `M=1`，`C=0` 的假设）；
- 增加强耦合选项（子迭代或 `Aitken` 加速）；
- 补充更多单元测试与回归测试（例如在理想化几何上验证模态投影与力平衡）。

## 附录 A：示例 `dynamicMeshDict` 与 `controlDict` 片段

`controlDict` 示例（system/controlDict`）：

```c
FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      controlDict;
}
application     pimpleFoam;
libs            ("libfastDynamicFvMesh.so");
startFrom       startTime;
startTime       0;
stopAt          endTime;
endTime         5.0;
deltaT          0.001;
adjustTimeStep  yes;
maxCo           1.0;
maxDeltaT       0.005;
writeControl    runTime;
writeInterval   0.1;
purgeWrite      0;
writeFormat     ascii;
writePrecision  8;
writeCompression off;
timeFormat      general;
timePrecision   6;
runTimeModifiable true;

```

constant/dynamicMeshDict 示例（关键项）：

```c
FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      dynamicMeshDict;
}

dynamicFvMesh fastDynamicFvMesh;

fastDynamicFvMeshCoeffs
{
    theta              1.4;
    fsiPatches         ("FLUID_FLUID_STRUCTURE_INTERFACE");
    mappingTolerance   4e-6;
    couplingRelaxation 0.1;
    pressureField      p;
    rhoRef             1000;
    writeFaceDiagnostics yes;
    faceDiagnosticsMode 4; // 若不需要面级输出，设为 -1
    modalFilesPath     "./modes/"; // 自定义键，指向模态文件目录（如果实现中存在）
}
```

## 附录 B：源代码（注释已翻译为中文）

文件：`fastDynamicFvMesh.H`

```cpp
/*----------------------------------------------------------------***********\
  =========                 |
  \\      /  F ield         | OpenFOAM: The Open Source CFD Toolbox
   \\    /   O peration     | Website:  https://openfoam.org
    \\  /    A nd           | Copyright (C) 2023 OpenFOAM Foundation
     \\/     M anipulation  |
 -------------------------------------------------------------------------------
 描述
     基于模态叠加的快速动网格方法。

 \*---------------------------------------------------------------------------*/

#ifndef fastDynamicFvMesh_H
#define fastDynamicFvMesh_H

#include "dynamicFvMesh.H"
#include "fieldTypes.H"

// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *

namespace Foam
{

/*---------------------------------------------------------------------------*\
                       Class fastDynamicFvMesh Declaration
\*---------------------------------------------------------------------------*/

class fastDynamicFvMesh
:
    public dynamicFvMesh
{
    // Private Data

        //- 模态数量
        label nMode_;

        //- 模态频率（Hz）
        scalarField modeFreq_;

        //- 模态阻尼比（无量纲）
        scalarField modeDamp_;

        //- 模态等效刚度（可由频率和质量推导）
        scalarField modeStiff_;

        //- 当前时间步的模态力（总和：压力+剪切），单位 N
        scalarField modeForce_;

        //- 投影到模态的压力分量
        scalarField modePressureForce_;

        //- 投影到模态的粘性/剪切分量
        scalarField modeShearForce_;

        //- 上一时间步的模态力（用于时间积分历史项）
        scalarField modeForce0_;

        //- 当前时间步的模态状态：layout 为 (disp, vel, acc)
        //  说明: modeState_[i].x() = 位移 (m), .y() = 速度 (m/s), .z() = 加速度 (m/s^2)
        List<vector> modeState_;

        //- 上一时间步的模态状态（历史项）
        List<vector> modeState0_;

        //- 初始模态速度（若存在 legacy 输入文件则读取）
        scalarField initVelocity_;

        //- 实际施加到网格的经过松弛的模态位移
        //  说明: 在显式耦合时使用此字段保存应用到网格的位移值（经过 couplingRelaxation_ 松弛）。
        scalarField appliedModeDisp_;

        //- 每个模态对应的形状向量场（对每个本地网格点存储位移向量）
        List<vectorField> modeShapes_;

        //- Wilson-Theta 时间积分参数
        scalar theta_;

        //- 用于 CSV->网格点匹配的坐标容差（米）
        scalar mappingTolerance_;

        //- 将结构求解产生的位移应用到网格前的松弛系数（alpha），取值 (0,1]
        scalar couplingRelaxation_;

        //- 用于计算模态载荷的压力字段名（通常为 "p"）
        word pressureFieldName_;

        //- 当压力为运动学压力（p/rho）时用于恢复密度的参考值
        scalar rhoRef_;

        //- 指定流固耦合界面的 patch 名称列表
        wordRes fsiPatches_;

        //- 是否写出每个面级别的模态力诊断
        bool writeFaceDiagnostics_;

        //- 面级诊断对应的模态索引（内部为 0-based，dynamicMeshDict 中以 1-based 指定）
        label faceDiagnosticsMode_;

        //- 启动阶段计数（用于特殊初始化，匹配 legacy UDF 行为）
        label startupStepCount_;

        //- 上一次已处理的物理时间索引（避免同一时间步重复 update）
        label lastUpdateTimeIndex_;


    // Private Member Functions

        //- 从字典和文件读取运行控制参数
        void readControls();

        //- 从外部文件读取模态形状
        void readModeShapes();

        //- 从 legacy 文件 FluidPara.csv 读取老接口参数（仅当文件存在时）
        void readLegacyParameters(const fileName& modeDir);

        //- 从流体压力和剪切应力计算模态力投影
        void calcModalForces();

        //- 返回用于剪切计算的 patch 密度数组（若存在 rho 字段则使用之，否则用默认值填充）
        tmp<scalarField> patchDensity(const label patchi, scalar defaultRho) const;

        //- 计算用于壁面剪切评价的偏 deviatoric 有效应力张量
        tmp<symmTensorField> devRhoReff
        (
            const tensorField& gradUp,
            const label patchi,
            scalar defaultRho
        ) const;

        //- 使用 Wilson-Theta 方法求解模态动力学方程
        void solveStructuralDynamics(scalar dt);

        //- 写出模态力/位移诊断
        void writeDiagnostics() const;

        //- 禁用默认的位拷贝构造
        fastDynamicFvMesh(const fastDynamicFvMesh&);

        //- 禁用默认的赋值操作
        void operator=(const fastDynamicFvMesh&);


public:

    //- 运行时类型信息
    TypeName("fastDynamicFvMesh");


    // Constructors

        //- 由 IOobject 构造
        explicit fastDynamicFvMesh(const IOobject& io);


    //- 析构函数
    virtual ~fastDynamicFvMesh();


    // Member Functions

        //- 更新网格（网格运动与拓扑变化）
        virtual bool update();
};


// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *

} // End namespace Foam

#endif

// ************************************************************************* //
```

---

文件：`fastDynamicFvMesh.C`

```cpp
/*----------------------------------------------------------------***********\
  =========                 |
  \\      /  F ield         | OpenFOAM: The Open Source CFD Toolbox
   \\    /   O peration     | Website:  https://openfoam.org
    \\  /    A nd           | Copyright (C) 2023 OpenFOAM Foundation
     \\/     M anipulation  |
 -------------------------------------------------------------------------------
 描述
     快速动网格方法实现（基于模态叠加）。

 \*---------------------------------------------------------------------------*/

#include "fastDynamicFvMesh.H"
#include "addToRunTimeSelectionTable.H"
#include "volFields.H"
#include "surfaceFields.H"
#include "fvcGrad.H"
#include "turbulentTransportModel.H"
#include "turbulentFluidThermoModel.H"
#include "fluidThermo.H"
#include "transportModel.H"
#include "syncTools.H"
#include "SortableList.H"
#include "Pstream.H"
#include <fstream>
#include <sstream>
#include <iomanip>
#include <algorithm>

// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *

namespace Foam
{

// 静态数据成员

defineTypeNameAndDebug(fastDynamicFvMesh, 0);
addToRunTimeSelectionTable(dynamicFvMesh, fastDynamicFvMesh, IOobject);

// 构造函数
// 目的：初始化 fastDynamicFvMesh 实例并加载运行时控制与模态形状。
// 说明：
//  - 初始化列表（dynamicFvMesh(io), ...）在函数体执行前设置默认成员值。
//  - readControls() 解析 constant/dynamicMeshDict 并验证必需字段（例如 fsiPatches）。
//  - readModeShapes() 加载 mode 目录下的 CSV 文件并建立结构节点到网格点的映射（暴力搜索以保证正确性）。
fastDynamicFvMesh::fastDynamicFvMesh(const IOobject& io)
:
    dynamicFvMesh(io),
    nMode_(0),
    theta_(1.4), // 默认 Wilson-Theta
    mappingTolerance_(4e-6),
    couplingRelaxation_(1.0),
    pressureFieldName_("p"),
    rhoRef_(-1.0),
    writeFaceDiagnostics_(false),
    faceDiagnosticsMode_(-1),
    startupStepCount_(0),
    lastUpdateTimeIndex_(-1)
{
    readControls();
    readModeShapes();
}

// 析构函数
// 目的：释放资源（当前实现依赖 RAII，因此无需显式清理）。
fastDynamicFvMesh::~fastDynamicFvMesh()
{}

// 解析 dynamicMeshDict 子字典并初始化控制参数
void fastDynamicFvMesh::readControls()
{
    // 打开 constant/dynamicMeshDict 中的字典对象并读取子字典 <typeName>Coeffs
    IOdictionary dynamicMeshDict
    (
        IOobject
        (
            "dynamicMeshDict",
            this->time().constant(),
            *this,
            IOobject::MUST_READ_IF_MODIFIED,
            IOobject::NO_WRITE,
            false
        )
    );

    if (!dynamicMeshDict.found(typeName + "Coeffs"))
    {
        FatalIOErrorInFunction(dynamicMeshDict)
            << "Missing required sub-dictionary '" << typeName << "Coeffs' in "
            << dynamicMeshDict.objectPath() << nl
            << "Add constant/dynamicMeshDict entries for " << typeName << '.'
            << exit(FatalIOError);
    }

    const dictionary& fdmDict = dynamicMeshDict.subDict(typeName + "Coeffs");

    // 读取可选参数
    fdmDict.readIfPresent("theta", theta_);

    // fsiPatches 为必填项，指定流固界面 patch 列表
    if (!fdmDict.found("fsiPatches"))
    {
        FatalIOErrorInFunction(dynamicMeshDict)
            << "Missing required entry 'fsiPatches' in sub-dictionary '"
            << typeName << "Coeffs' of " << dynamicMeshDict.objectPath()
            << exit(FatalIOError);
    }

    fdmDict.lookup("fsiPatches") >> fsiPatches_;

    if (!fsiPatches_.size())
    {
        FatalErrorInFunction
            << "At least one FSI patch name must be provided in "
            << dynamicMeshDict.objectPath() << exit(FatalError);
    }

    // 读取其他可选控制项
    fdmDict.readIfPresent("mappingTolerance", mappingTolerance_);
    fdmDict.readIfPresent("couplingRelaxation", couplingRelaxation_);
    fdmDict.readIfPresent("pressureField", pressureFieldName_);
    const bool foundRhoRef = fdmDict.readIfPresent("rhoRef", rhoRef_);

    // 校验 couplingRelaxation_ 范围
    if (couplingRelaxation_ <= 0 || couplingRelaxation_ > 1)
    {
        FatalIOErrorInFunction(dynamicMeshDict)
            << "Entry 'couplingRelaxation' must be in the range (0, 1] in "
            << "sub-dictionary '" << typeName << "Coeffs' of "
            << dynamicMeshDict.objectPath() << exit(FatalIOError);
    }

    if (foundRhoRef && rhoRef_ <= 0)
    {
        FatalIOErrorInFunction(dynamicMeshDict)
            << "Entry 'rhoRef' must be positive in sub-dictionary '"
            << typeName << "Coeffs' of " << dynamicMeshDict.objectPath()
            << exit(FatalIOError);
    }

    // 处理面级诊断设置（faceDiagnosticsMode）
    label faceDiagnosticsMode = -1;
    const bool foundFaceDiagnosticsMode =
        fdmDict.readIfPresent("faceDiagnosticsMode", faceDiagnosticsMode);

    fdmDict.readIfPresent("writeFaceDiagnostics", writeFaceDiagnostics_);

    if (foundFaceDiagnosticsMode)
    {
        writeFaceDiagnostics_ = true;
        // dynamicMeshDict 中以 1-based 指定，内部转换为 0-based
        faceDiagnosticsMode_ = faceDiagnosticsMode - 1;
    }

    if (writeFaceDiagnostics_ && faceDiagnosticsMode_ < 0)
    {
        FatalIOErrorInFunction(dynamicMeshDict)
            << "When 'writeFaceDiagnostics' is enabled, provide a positive "
            << "'faceDiagnosticsMode' entry in sub-dictionary '"
            << typeName << "Coeffs' of " << dynamicMeshDict.objectPath()
            << exit(FatalIOError);
    }
}

// 在主进程上读取 legacy 参数文件 mode/FluidPara.csv（如果存在）
void fastDynamicFvMesh::readLegacyParameters(const fileName& modeDir)
{
    if (!Pstream::master())
    {
        return;
    }

    fileName paraPath = modeDir/"FluidPara.csv";
    std::ifstream paraFile(paraPath);

    if (!paraFile.good())
    {
        Info<< "Legacy parameter file " << paraPath
            << " not found; using zero initial modal velocities." << endl;
        return;
    }

    // 辅助函数：从 CSV 中读取下一个可解析的标量（忽略逗号/空格）
    auto readCsvScalar = [&](scalar& value) -> bool
    {
        string line;

        while (std::getline(paraFile, line))
        {
            std::replace(line.begin(), line.end(), ',', ' ');
            std::stringstream ss(line);

            if (ss >> value)
            {
                return true;
            }
        }

        return false;
    };

    scalar legacyFsiId = -1;
    scalar legacyFluidId = -1;

    if (!readCsvScalar(legacyFsiId) || !readCsvScalar(legacyFluidId))
    {
        WarningInFunction
            << "Cannot read legacy ids from " << paraPath
            << ". Initial modal velocities remain zero." << endl;

        return;
    }

    Info<< "Read legacy FluidPara.csv ids (fsi=" << legacyFsiId
        << ", fluid=" << legacyFluidId
        << "). OpenFOAM uses patch names from dynamicMeshDict instead."
        << endl;

    for (label modeI = 0; modeI < nMode_; ++modeI)
    {
        scalar value = 0.0;

        if (!readCsvScalar(value))
        {
            WarningInFunction
                << "Missing initial velocity for mode " << modeI
                << " in " << paraPath
                << ". Remaining initial velocities stay at zero." << endl;

            break;
        }

        initVelocity_[modeI] = value;
    }
}

// 从 mode 目录读取模态坐标和模态形状，并将其映射到本地网格点
void fastDynamicFvMesh::readModeShapes()
{
    // 1. 在 master 进程上读取文件
    List<point> csvPoints;
    List<List<vector>> csvShapes;
    label nCsvNodes = 0;
    fileName modeDir = this->time().path()/"mode";

    if (Pstream::parRun() && !exists(modeDir))
    {
        modeDir = this->time().path().path()/"mode";
    }

    if (Pstream::master())
    {
        Info<< "Reading mode coordinates..." << endl;

        fileName coorPath = modeDir/"FluidNodeCoor.csv";
        Info<< "Trying to open: " << coorPath << endl;

        std::ifstream file(coorPath);
        if (!file.good())
        {
            FatalErrorInFunction
                << "Cannot open required mode coordinate file " << coorPath << nl
                << "Provide mode/FluidNodeCoor.csv in the case root before "
                << "running fastDynamicFvMesh." << exit(FatalError);
        }
        else
        {
            scalar dummy, nNode, nMode;
            // 稳健解析 header 行（支持逗号或空格分隔）
            string line;
            std::getline(file, line);
            std::replace(line.begin(), line.end(), ',', ' ');
            std::stringstream ss(line);
            if (!(ss >> dummy >> nNode >> nMode) || nNode <= 0 || nMode <= 0)
            {
                FatalErrorInFunction
                    << "Invalid FluidNodeCoor.csv header in " << coorPath << nl
                    << "Expected three comma-separated values with positive "
                    << "node and mode counts." << exit(FatalIOError);
            }

            nCsvNodes = label(nNode);
            nMode_ = label(nMode);

            Info<< "  Found " << nCsvNodes << " nodes and " << nMode_ << " modes in CSV." << endl;

            modeFreq_.setSize(nMode_); // 在 master 上调整大小
            csvPoints.setSize(nCsvNodes);
            csvShapes.setSize(nMode_);
            forAll(csvShapes, m) csvShapes[m].setSize(nCsvNodes);

            // 读取坐标数据
            for (label i=0; i<nCsvNodes; ++i)
            {
                scalar x, y, z;
                // 稳健解析坐标行（将逗号替换为空格后读取数值）
                if (!std::getline(file, line))
                {
                    FatalErrorInFunction
                        << "Unexpected end of file while reading node " << i
                        << " from " << coorPath << exit(FatalError);
                }
                std::replace(line.begin(), line.end(), ',', ' ');
                std::stringstream ss2(line);
                if (!(ss2 >> x >> y >> z))
                {
                    FatalErrorInFunction
                        << "Invalid coordinate entry for node " << i << " in "
                        << coorPath << exit(FatalError);
                }

                csvPoints[i] = point(x, y, z);
            }

            // 读取每个模态的形状文件
            for (label m=0; m<nMode_; ++m)
            {
                fileName shapePath = modeDir/("FluidNodeDisp" + std::to_string(m+1) + ".csv");
                std::ifstream mFile(shapePath);

                if (!mFile.good())
                {
                    FatalErrorInFunction
                        << "Cannot open required mode shape file " << shapePath
                        << exit(FatalError);
                }

                string line;
                if (!std::getline(mFile, line))
                {
                    FatalErrorInFunction
                        << "Missing frequency line in " << shapePath
                        << exit(FatalError);
                }

                std::replace(line.begin(), line.end(), ',', ' ');
                std::stringstream ss(line);
                scalar freq = 0.0;
                scalar fileNodeCount = 0.0;
                scalar fileModeCount = 0.0;

                if (!(ss >> freq))
                {
                    FatalErrorInFunction
                        << "Failed to read frequency from " << shapePath
                        << exit(FatalError);
                }

                if ((ss >> fileNodeCount) && (ss >> fileModeCount))
                {
                    if
                    (
                        label(fileNodeCount) != nCsvNodes
                     || label(fileModeCount) != nMode_
                    )
                    {
                        FatalErrorInFunction
                            << "Mode file " << shapePath
                            << " reports " << label(fileNodeCount)
                            << " nodes and " << label(fileModeCount)
                            << " modes, but FluidNodeCoor.csv reports "
                            << nCsvNodes << " nodes and " << nMode_
                            << " modes." << exit(FatalIOError);
                    }
                }

                modeFreq_[m] = freq;
                Info<< "  Mode " << m << " Freq: " << freq << endl;

                label dataRow = 0;

                while (dataRow < nCsvNodes)
                {
                    if (!std::getline(mFile, line))
                    {
                        FatalErrorInFunction
                            << "Unexpected end of file while reading node "
                            << dataRow << " from " << shapePath
                            << exit(FatalError);
                    }

                    std::replace(line.begin(), line.end(), ',', ' ');
                    std::stringstream ss2(line);

                    scalar dx = 0.0;
                    scalar dy = 0.0;
                    scalar dz = 0.0;

                    if (!(ss2 >> dx >> dy >> dz))
                    {
                        if (dataRow == 0)
                        {
                            // 如果第一行非数值（例如 header），跳过并继续
                            continue;
                        }

                        FatalErrorInFunction
                            << "Invalid displacement entry for node "
                            << dataRow << " in " << shapePath
                            << exit(FatalError);
                    }

                    csvShapes[m][dataRow] = vector(dx, dy, dz);
                    ++dataRow;
                }
            }
        }
    }

    // 广播大小信息到所有进程
    Pstream::broadcast(nMode_);
    Pstream::broadcast(nCsvNodes);

    // 在从进程上调整 modeFreq_ 容量
    if (!Pstream::master()) modeFreq_.setSize(nMode_); // Only slaves need resize now

    if (nMode_ <= 0)
    {
        FatalErrorInFunction
            << "No fluid modes were loaded from " << modeDir << nl
            << "Ensure mode/FluidNodeCoor.csv and mode/FluidNodeDisp*.csv are "
            << "present and valid." << exit(FatalError);
    }

    if (writeFaceDiagnostics_ && faceDiagnosticsMode_ >= nMode_)
    {
        FatalErrorInFunction
            << "Requested faceDiagnosticsMode " << (faceDiagnosticsMode_ + 1)
            << " but only " << nMode_ << " modes were loaded."
            << exit(FatalError);
    }

    // 初始化模态数组
    modeForce_.setSize(nMode_, 0.0);
    modePressureForce_.setSize(nMode_, 0.0);
    modeShearForce_.setSize(nMode_, 0.0);
    modeForce0_.setSize(nMode_, 0.0);
    modeState_.setSize(nMode_, vector::zero);
    modeState0_.setSize(nMode_, vector::zero);
    initVelocity_.setSize(nMode_, 0.0);
    appliedModeDisp_.setSize(nMode_, 0.0);

    readLegacyParameters(modeDir);

    // 广播频率、坐标和初始速度到所有进程
    Pstream::broadcast(modeFreq_);
    Pstream::broadcast(csvPoints);
    Pstream::broadcast(initVelocity_);

    // 在所有进程上调整 csvShapes 的尺寸并广播每个模态形状
    csvShapes.setSize(nMode_);

    forAll(csvShapes, m) Pstream::broadcast(csvShapes[m]);

    // 将 csvShapes 映射到本地网格点（modeShapes_）
    const pointField& localPoints = this->points();
    modeShapes_.setSize(nMode_);
    forAll(modeShapes_, m)
    {
        modeShapes_[m].setSize(localPoints.size());
        modeShapes_[m] = vector::zero;
    }

    // 暴力最近邻搜索：将 csvPoints 映射到本地网格点
    label mappedCount = 0;
    if (nCsvNodes > 0)
    {
        scalar tolSqr = sqr(mappingTolerance_);

        forAll(localPoints, pI)
        {
            const point& p = localPoints[pI];
            scalar minDistSqr = GREAT;
            label nearestIdx = -1;

            forAll(csvPoints, cI)
            {
                scalar d = magSqr(p - csvPoints[cI]);
                if (d < minDistSqr)
                {
                    minDistSqr = d;
                    nearestIdx = cI;
                }
            }

            if (minDistSqr < tolSqr && nearestIdx != -1)
            {
                mappedCount++;
                for (label m=0; m<nMode_; ++m)
                {
                    modeShapes_[m][pI] = csvShapes[m][nearestIdx];
                }
            }
        }
    }

    const label totalMapped = returnReduce(mappedCount, sumOp<label>());
    const label totalPoints = returnReduce(label(localPoints.size()), sumOp<label>());

    if (Pstream::master())
    {
        Info<< "Mapped " << totalMapped << " points out of "
            << totalPoints << " mesh points." << endl;
    }

    if (totalMapped == 0)
    {
        FatalErrorInFunction
            << "Mapped 0 mesh points from " << modeDir << nl
            << "Check that the mode CSV coordinates correspond to the current "
            << "mesh and that mappingTolerance is large enough." << exit(FatalError);
    }

    if (Pstream::master() && totalMapped != totalPoints)
    {
        WarningInFunction
            << "Mapped " << totalMapped << " of " << totalPoints
            << " mesh points. Unmapped points will remain stationary."
            << endl;
    }
}

// patchDensity(patchi, defaultRho)
// 目的：返回给定 patch 的密度数组；若存在 volScalarField "rho" 则使用其边界字段，否则用默认值填充。
tmp<scalarField> fastDynamicFvMesh::patchDensity
(
    const label patchi,
    const scalar defaultRho
) const
{
    if (this->foundObject<volScalarField>("rho"))
    {
        const auto& rhoField = this->lookupObject<volScalarField>("rho");
        return tmp<scalarField>
        (
            new scalarField(rhoField.boundaryField()[patchi])
        );
    }

    return tmp<scalarField>
    (
        new scalarField(this->boundaryMesh()[patchi].size(), defaultRho)
    );
}

// devRhoReff(gradUp, patchi, defaultRho)
// 目的：计算用于壁面剪切评估的偏 deviatoric 有效应力张量，优先使用可用的湍流/传输模型获取有效黏性。
tmp<symmTensorField> fastDynamicFvMesh::devRhoReff
(
    const tensorField& gradUp,
    const label patchi,
    const scalar defaultRho
) const
{
    typedef incompressible::turbulenceModel icoTurbModel;
    typedef compressible::turbulenceModel cmpTurbModel;

    if (this->foundObject<icoTurbModel>(icoTurbModel::propertiesName))
    {
        const auto& turb =
            this->lookupObject<icoTurbModel>(icoTurbModel::propertiesName);

        tmp<scalarField> tRho = patchDensity(patchi, defaultRho);

        return tmp<symmTensorField>
        (
            new symmTensorField
            (
                -tRho()*turb.nuEff(patchi)*devTwoSymm(gradUp)
            )
        );
    }
    else if (this->foundObject<cmpTurbModel>(cmpTurbModel::propertiesName))
    {
        const auto& turb =
            this->lookupObject<cmpTurbModel>(cmpTurbModel::propertiesName);

        return tmp<symmTensorField>
        (
            new symmTensorField
            (
                -turb.muEff(patchi)*devTwoSymm(gradUp)
            )
        );
    }
    else if (this->foundObject<fluidThermo>(fluidThermo::dictName))
    {
        const auto& thermo = this->lookupObject<fluidThermo>(fluidThermo::dictName);

        return tmp<symmTensorField>
        (
            new symmTensorField
            (
                -thermo.mu(patchi)*devTwoSymm(gradUp)
            )
        );
    }
    else if (this->foundObject<transportModel>("transportProperties"))
    {
        const auto& laminarT =
            this->lookupObject<transportModel>("transportProperties");

        tmp<scalarField> tRho = patchDensity(patchi, defaultRho);

        return tmp<symmTensorField>
        (
            new symmTensorField
            (
                -tRho()*laminarT.nu(patchi)*devTwoSymm(gradUp)
            )
        );
    }
    else
    {
        IOdictionary transportProperties
        (
            IOobject
            (
                "transportProperties",
                this->time().constant(),
                *this,
                IOobject::MUST_READ,
                IOobject::NO_WRITE
            )
        );

        const dimensionedScalar nu("nu", dimViscosity, transportProperties);
        tmp<scalarField> tRho = patchDensity(patchi, defaultRho);

        return tmp<symmTensorField>
        (
            new symmTensorField
            (
                -tRho()*nu.value()*devTwoSymm(gradUp)
            )
        );
    }
}

// calcModalForces()
// 目的：通过将面力（压力与剪切）投影到模态形状上，计算每个模态的广义力（并分别累加压力/剪切贡献以便诊断）。
void fastDynamicFvMesh::calcModalForces()
{
    // 初始化模态力累加器
    modeForce_ = 0.0;
    modePressureForce_ = 0.0;
    modeShearForce_ = 0.0;

    // 确保压力场存在（支持物理压力或运动学压力）
    if (!this->foundObject<volScalarField>(pressureFieldName_))
    {
        FatalErrorInFunction
            << "Required pressure field '" << pressureFieldName_
            << "' was not found. Configure 'pressureField' in dynamicMeshDict "
            << "or supply the field before fastDynamicFvMesh::update()."
            << exit(FatalIOError);
    }

    const volScalarField& p = this->lookupObject<volScalarField>(pressureFieldName_);
    const dimensionSet kinematicPressureDims(dimPressure/dimDensity);
    const bool dimensionalPressure = (p.dimensions() == dimPressure);
    const bool kinematicPressure = (p.dimensions() == kinematicPressureDims);

    if (!dimensionalPressure && !kinematicPressure)
    {
        FatalErrorInFunction
            << "Pressure field '" << pressureFieldName_ << "' has dimensions "
            << p.dimensions() << ". Expected either " << dimPressure
            << " (pressure) or " << kinematicPressureDims
            << " (kinematic pressure)." << exit(FatalIOError);
    }

    scalar defaultRho = rhoRef_;

    // 若压力为运动学压力且未给出 rhoRef，则从 transportProperties 或场 'rho' 中恢复
    if (kinematicPressure && defaultRho <= 0)
    {
        IOdictionary transportProperties
        (
            IOobject
            (
                "transportProperties",
                this->time().constant(),
                *this,
                IOobject::MUST_READ,
                IOobject::NO_WRITE
            )
        );

        if (transportProperties.found("rho"))
        {
            transportProperties.lookup("rho") >> defaultRho;
        }
    }

    if (kinematicPressure && defaultRho <= 0 && !this->foundObject<volScalarField>("rho"))
    {
        FatalErrorInFunction
            << "Pressure field '" << pressureFieldName_
            << "' is kinematic, but no density information was found." << nl
            << "Provide 'rhoRef' in dynamicMeshDict, a 'rho' entry in "
            << "constant/transportProperties, or a volScalarField named 'rho'."
            << exit(FatalIOError);
    }

    std::ofstream faceDiagFile;
    bool writeFaceDiagHeader = false;
    bool haveFaceDiagFile = false;

    // 如果启用面级诊断，则打开对应的输出文件（按进程编号分文件）
    if (writeFaceDiagnostics_)
    {
        fileName diagnosticsRoot = this->time().path();

        if (Pstream::parRun())
        {
            diagnosticsRoot = this->time().path().path();
        }

        const word modeLabel("mode" + Foam::name(faceDiagnosticsMode_ + 1));
        fileName faceDiagnosticsPath =
            diagnosticsRoot/
            ("faceDiagnostics_" + modeLabel + "_proc"
           + Foam::name(Pstream::myProcNo()) + ".csv");

        std::ifstream check(faceDiagnosticsPath.c_str());
        writeFaceDiagHeader = !check.good();
        check.close();

        faceDiagFile.open(faceDiagnosticsPath.c_str(), std::ios::app);

        if (!faceDiagFile.good())
        {
            WarningInFunction
                << "Unable to open " << faceDiagnosticsPath
                << " for face-level diagnostics." << endl;
        }
        else
        {
            haveFaceDiagFile = true;
            faceDiagFile << std::setprecision(12);

            if (writeFaceDiagHeader)
            {
                // CSV 文件头（保持原始英文列名以便后处理脚本兼容）
                faceDiagFile
                    << "Processor,Time,Patch,PatchFace,MeshFace,"
                    << "Cx,Cy,Cz,"
                    << "AreaX,AreaY,AreaZ,"
                    << "ShapeX,ShapeY,ShapeZ,"
                    << "PressureForceX,PressureForceY,PressureForceZ,"
                    << "ShearForceX,ShearForceY,ShearForceZ,"
                    << "PressureContribution,ShearContribution,TotalContribution\n";
            }
        }
    }

    tmp<volTensorField> tGradU;
    const volTensorField* gradUPtr = nullptr;

    // 若存在速度场 U，则计算其梯度用于评估粘性应力，若不存在则跳过剪切项
    if (this->foundObject<volVectorField>("U"))
    {
        const volVectorField& U = this->lookupObject<volVectorField>("U");
        tGradU = fvc::grad(U);
        gradUPtr = &tGradU();
    }
    else
    {
        WarningInFunction
            << "Velocity field 'U' not found; wall shear contribution "
            << "to modal forces will be skipped." << endl;
    }

    // 调试压力范围
    scalar pMin = gMin(p);
    scalar pMax = gMax(p);
    if (Pstream::master())
    {
        Info << "DEBUG: " << pressureFieldName_ << " range [" << pMin << ", "
             << pMax << "]" << endl;

        if (kinematicPressure)
        {
            Info << "DEBUG: pressure field is kinematic; density comes from "
                 << (this->foundObject<volScalarField>("rho") ? "field rho" : "rhoRef/transportProperties")
                 << endl;
        }
    }

    // 遍历所有配置的 FSI patch，积累面力并投影到模态
    forAll(fsiPatches_, i)
    {
        label patchID = this->boundaryMesh().findPatchID(fsiPatches_[i]);
        if (patchID == -1)
        {
            FatalErrorInFunction
                << "Configured FSI patch '" << fsiPatches_[i]
                << "' was not found in boundaryMesh." << exit(FatalError);
        }

        const polyPatch& pp = this->boundaryMesh()[patchID];
        const fvPatchScalarField& pPatch = p.boundaryField()[patchID];
        const vectorField faceAreas(pp.faceAreas());
        const vectorField faceCentres(pp.faceCentres());

        tmp<scalarField> tPressureScale
        (
            new scalarField(pp.size(), 1.0)
        );

        if (kinematicPressure)
        {
            // 对于运动学压力，使用密度恢复真实压力
            tPressureScale = patchDensity(patchID, defaultRho);
        }

        const scalarField& pressureScale = tPressureScale();

        const symmTensorField* devStressPtr = nullptr;
        tmp<symmTensorField> tDevStress;

        if (gradUPtr)
        {
            const tensorField& gradPatch = gradUPtr->boundaryField()[patchID];
            tDevStress = devRhoReff(gradPatch, patchID, defaultRho > 0 ? defaultRho : 1.0);
            devStressPtr = &tDevStress();
        }

        // 遍历每个面
        forAll(pp, faceI)
        {
            const vector& areaVec = faceAreas[faceI];
            const vector pressureForce = pressureScale[faceI] * pPatch[faceI] * areaVec;
            vector shearForce = vector::zero;

            if (devStressPtr)
            {
                // 直接使用应力张量与面积向量相乘得到面力矢量
                shearForce = areaVec & (*devStressPtr)[faceI];
                const scalar areaMag = mag(areaVec);

                if (areaMag > VSMALL)
                {
                    // 去除法向分量，只保留切向剪切
                    const vector faceNormal = areaVec/areaMag;
                    shearForce -= faceNormal*(faceNormal & shearForce);
                }
            }

            // 获取面上的顶点索引以便插值模态形状
            const labelList& fPoints = pp[faceI];

            for (label m=0; m<nMode_; ++m)
            {
                // 在面心对模态形状取平均
                vector shapeFace = vector::zero;
                forAll(fPoints, fp)
                {
                    shapeFace += modeShapes_[m][fPoints[fp]];
                }
                if (fPoints.size() > 0)
                {
                    shapeFace /= fPoints.size();
                }

                // 模态力贡献为 面力 与 形状向量 的点积
                const scalar pressureModeForce = pressureForce & shapeFace;
                const scalar shearModeForce = shearForce & shapeFace;

                modePressureForce_[m] += pressureModeForce;
                modeShearForce_[m] += shearModeForce;
                modeForce_[m] += pressureModeForce + shearModeForce;

                // 若启用了面级诊断且当前模态匹配设置的模态，则写出该面的详细信息
                if (haveFaceDiagFile && m == faceDiagnosticsMode_)
                {
                    const point& faceCentre = faceCentres[faceI];

                    faceDiagFile
                        << Pstream::myProcNo() << ','
                        << this->time().value() << ','
                        << pp.name() << ','
                        << faceI << ','
                        << (pp.start() + faceI) << ','
                        << faceCentre.x() << ','
                        << faceCentre.y() << ','
                        << faceCentre.z() << ','
                        << areaVec.x() << ','
                        << areaVec.y() << ','
                        << areaVec.z() << ','
                        << shapeFace.x() << ','
                        << shapeFace.y() << ','
                        << shapeFace.z() << ','
                        << pressureForce.x() << ','
                        << pressureForce.y() << ','
                        << pressureForce.z() << ','
                        << shearForce.x() << ','
                        << shearForce.y() << ','
                        << shearForce.z() << ','
                        << pressureModeForce << ','
                        << shearModeForce << ','
                        << (pressureModeForce + shearModeForce) << '\n';
                }
            }
        }
    }

    // 并行归约：将每个进程上的模态累加器合并
    Pstream::listCombineGather(modeForce_, plusEqOp<scalar>());
    Pstream::listCombineGather(modePressureForce_, plusEqOp<scalar>());
    Pstream::listCombineGather(modeShearForce_, plusEqOp<scalar>());
    Pstream::broadcast(modeForce_);
    Pstream::broadcast(modePressureForce_);
    Pstream::broadcast(modeShearForce_);

    if (Pstream::master())
    {
        Info << "DEBUG: Mode Forces p/shear/total (0-4): ";
        for (label i=0; i<min(5, nMode_); ++i)
        {
            Info << '['
                 << modePressureForce_[i] << ','
                 << modeShearForce_[i] << ','
                 << modeForce_[i] << "] ";
        }
        Info << endl;
    }
}

// solveStructuralDynamics(dt)
// 目的：对每个模态独立积分模态方程并更新 modeState_（位移、速度、加速度）。
// 说明：当前实现使用 Wilson-Theta 单步积分，且质量与阻尼暂时使用硬编码值（Mass=1, Damp=0）。
void fastDynamicFvMesh::solveStructuralDynamics(scalar dt)
{
    // 目前的遗留假定：单位模态质量且无阻尼
    const scalar Mass = 1.0;
    const scalar Damp = 0.0;

    for (label i=0; i<nMode_; ++i)
    {
        scalar dis = 0, vel = 0, acc = 0;

        scalar disLast = modeState0_[i].x();
        scalar velLast = modeState0_[i].y();
        scalar accLast = modeState0_[i].z();

        scalar F_this = modeForce_[i];
        scalar F_last = modeForce0_[i];
        scalar freq = modeFreq_[i];

        scalar omega = 2.0 * constant::mathematical::pi * freq;

        // 构造有效刚度项，包含 Wilson-Theta 时间离散产生的贡献
        scalar effectiveK = 6.0*Mass/sqr(theta_*dt) + 3.0*Damp/(theta_*dt) + sqr(omega);

        // 计算加载项：包含 theta 加权的力和历史项
        scalar load = F_last + theta_*(F_this - F_last);
        load += Mass * (6.0/sqr(theta_*dt)*disLast + 6.0/(theta_*dt)*velLast + 2.0*accLast);
        load += Damp * (3.0/(theta_*dt)*disLast + 2.0*velLast + 0.5*theta_*dt*accLast);

        // 求解 theta 时刻的位移
        scalar disTheta = load / effectiveK;

        scalar tau = theta_ * dt;
        scalar term1 = 6.0 / (sqr(tau) * theta_) * (disTheta - disLast);
        scalar term2 = 6.0 / (sqr(theta_) * dt) * velLast;
        scalar term3 = (1.0 - 3.0/theta_) * accLast;
        acc = term1 - term2 + term3;

        vel = velLast + 0.5*dt*(acc + accLast);
        dis = disLast + dt*velLast + sqr(dt)/6.0*(acc + 2.0*accLast);

        modeState_[i] = vector(dis, vel, acc);
    }
}

// writeDiagnostics()
// 目的：在主进程（或并行运行时的 run 目录）将每时间步的模态诊断追加写入 modal_diagnostics.csv。
void fastDynamicFvMesh::writeDiagnostics() const
{
    if (!Pstream::master())
    {
        return;
    }

    fileName diagnosticsPath = this->time().path()/"modal_diagnostics.csv";

    if (Pstream::parRun())
    {
        diagnosticsPath = this->time().path().path()/"modal_diagnostics.csv";
    }

    std::ofstream diagFile;
    bool writeHeader = false;

    std::ifstream check(diagnosticsPath.c_str());
    if (!check.good())
    {
        writeHeader = true;
    }
    check.close();

    diagFile.open(diagnosticsPath.c_str(), std::ios::app);

    if (!diagFile.good())
    {
        WarningInFunction << "Unable to open " << diagnosticsPath
            << " for writing." << endl;
        return;
    }

    if (writeHeader)
    {
        // 写入 CSV 头部（列名保留英文以便兼容现有分析脚本）
        diagFile << "Time";
        for (label i=0; i<nMode_; ++i)
        {
            diagFile << ",Force_" << (i+1);
        }

        for (label i=0; i<nMode_; ++i)
        {
            diagFile << ",PressureForce_" << (i+1);
        }

        for (label i=0; i<nMode_; ++i)
        {
            diagFile << ",ShearForce_" << (i+1);
        }

        for (label i=0; i<nMode_; ++i)
        {
            diagFile << ",Disp_" << (i+1)
                     << ",Vel_" << (i+1)
                     << ",Acc_" << (i+1)
                     << ",AppliedDisp_" << (i+1);
        }

        diagFile << "\n";
    }

    diagFile << std::setprecision(12);
    diagFile << this->time().value();

    for (label i=0; i<nMode_; ++i)
    {
        diagFile << "," << modeForce_[i];
    }

    for (label i=0; i<nMode_; ++i)
    {
        diagFile << "," << modePressureForce_[i];
    }

    for (label i=0; i<nMode_; ++i)
    {
        diagFile << "," << modeShearForce_[i];
    }

    for (label i=0; i<nMode_; ++i)
    {
        diagFile << "," << modeState_[i].x()
                 << "," << modeState_[i].y()
                 << "," << modeState_[i].z()
                 << "," << appliedModeDisp_[i];
    }

    diagFile << "\n";
}

// update()
// 目的：由 OpenFOAM 每个时间步调用，计算模态力、求解结构动力学并移动网格。
// 流程概述：
//  1) 使用 lastUpdateTimeIndex_ 防止对同一物理时间重复更新；
//  2) 计算 dt 并保存上一时刻的模态状态/力用于时间积分；
//  3) 调用 calcModalForces() 组装当前模态载荷（分离压力与剪切以便诊断）；
//  4) 启动阶段特殊初始化（前两次 update() 用于匹配 legacy 行为）；
//  5) 使用 solveStructuralDynamics(dt) 得到当前模态位移/速度/加速度；
//  6) 应用 couplingRelaxation_ 计算 appliedModeDisp_ 并得到增量位移 dDisp；
//  7) 按模态形状重建网格点增量并调用 movePoints(newPoints)；
//  8) 写出诊断并返回 true 表示网格已被更新。
bool fastDynamicFvMesh::update()
{
    if (Pstream::master()) Info<< "DEBUG: Starting update()" << endl;

    const label currentTimeIndex = this->time().timeIndex();
    if (currentTimeIndex == lastUpdateTimeIndex_)
    {
        return false;
    }
    lastUpdateTimeIndex_ = currentTimeIndex;

    // 1. 计算时间步长 dt
    scalar dt = this->time().deltaTValue();

    // 保存上一步状态
    modeForce0_ = modeForce_;
    modeState0_ = modeState_;

    // 2. 计算模态力（压力 + 剪切）
    if (Pstream::master()) Info<< "DEBUG: Calling calcModalForces()" << endl;
    calcModalForces();
    if (Pstream::master()) Info<< "DEBUG: Finished calcModalForces()" << endl;

    // 启动初始化阶段：前两步使用特定的初始化以匹配 legacy 实现
    if (startupStepCount_ < 2)
    {
        if (Pstream::master())
        {
            Info<< "Initializing Fast Dynamic Mesh state (startup step "
                << (startupStepCount_ + 1) << " of 2)..." << endl;
        }

        for (label i=0; i<nMode_; ++i)
        {
            scalar F = modeForce_[i];
            scalar v = 0.0;
            if (i < initVelocity_.size()) v = initVelocity_[i];

            // 兼容 legacy Fluent UDF 的初始化：D0 = 0, V0 = initVelocity, A0 = F0
            scalar a = F;

            modeState_[i] = vector(0.0, v, a);
            modeState0_[i] = modeState_[i];
            modeForce0_[i] = modeForce_[i];
            appliedModeDisp_[i] = 0.0;
        }

        ++startupStepCount_;
        writeDiagnostics();

        if (Pstream::master())
        {
            Info<< "DEBUG: Skipping mesh motion during startup initialisation" << endl;
        }

        return true;
    }

    // 3. 求解模态动力学
    if (nMode_ > 0)
    {
        if (Pstream::master()) Info<< "DEBUG: Calling solveStructuralDynamics()" << endl;
        solveStructuralDynamics(dt);
        if (Pstream::master()) Info<< "DEBUG: Finished solveStructuralDynamics()" << endl;
    }

    // 4. 更新网格点
    if (Pstream::master()) Info<< "DEBUG: Updating mesh points" << endl;
    pointField newPoints = this->points();

    // 调试输出：打印第一模态的位移与已施加位移
    if (Pstream::master() && nMode_ > 0)
    {
        Info<< "Time: " << this->time().value()
            << " First Mode Disp: " << modeState_[0].x()
            << " Applied: " << appliedModeDisp_[0] << endl;
    }

    for (label m=0; m<nMode_; ++m)
    {
        const vectorField& shape = modeShapes_[m];

        const scalar appliedDisp0 = appliedModeDisp_[m];
        const scalar appliedDisp =
            appliedDisp0
          + couplingRelaxation_*(modeState_[m].x() - appliedDisp0);

        // 实际施加到网格的位移增量
        scalar dDisp = appliedDisp - appliedDisp0;
        appliedModeDisp_[m] = appliedDisp;

        forAll(newPoints, pI)
        {
            newPoints[pI] += dDisp * shape[pI];
        }
    }

    this->movePoints(newPoints);
    writeDiagnostics();

    return true;
}

} // End namespace Foam
```

---

注：为了保持源码的可直接使用性，`CSV`/诊断输出的列名和 `Info`/`Warning`/`Fatal` 消息均未改动，仅将注释文本转换为中文以便阅读。如需我把附录拆分成单独的文件（例如 `Doc/appendix-src-cn.md`）或者以不同的格式（`ZIP`、`PDF`）导出，请告诉我。
